{% extends 'base.html.twig' %}

{% block title "Recette : " ~ recipe.title %}

{% block body %}

    <p><img src="{{ vich_uploader_asset(recipe, 'thumbnailFile') }}" alt=""></p>

    <h1>{{ recipe.title }}</h1>
    <p>{{ recipe.content|nl2br }}</p>

{% endblock %}